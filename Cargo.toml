[package]
name = "sportanalytics"
version = "0.1.0"
edition = "2021"
rust-version = "1.71"
authors = ["Gunnar Karlsson <gunnar.h.karlsson@gmail.com>"]
description = "Running analytics in Rust: Daniels VDOT, race-time prediction, training zones, and age grading. Designed so other sports can be added later."
license = "MIT"
readme = "README.md"
repository = "https://github.com/GunnarKarlsson/sports-analytics"
homepage = "https://github.com/GunnarKarlsson/sports-analytics"
documentation = "https://docs.rs/sportanalytics"
keywords = ["running", "vo2max", "vdot", "age-grade", "race-prediction"]
categories = ["science"]
include = [
    "src/**/*",
    "examples/**/*",
    "Cargo.toml",
    "LICENSE",
    "README.md",
    "CHANGELOG.md",
]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs", "--generate-link-to-definition"]

[features]
default = []
serde = ["dep:serde"]

[dependencies]
serde = { version = "1", optional = true, default-features = false, features = ["derive", "std"] }

[dev-dependencies]
serde_json = "1"
